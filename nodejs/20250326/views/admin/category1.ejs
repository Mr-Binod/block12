


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>카태고리 추가</title>
</head>
<body>
    <%- include('../common/header') %>
    <% if(data) { %>
        <ul>
            <% for(let i = 0; i < data.length; i++) { %>
                <li> <%= i + 1 %> 번 카태고리<%= data[i].name %></li>
            <% } %>
        </ul>
            <div>데이터 있음</div>
        <% } %>
    <form action="" id="category1form">
        <label for="">카태고리 이름</label>
        <input type="text" name="category1">
        <button>category upload</button>
    </form>
</body>
<script>
    category1form.onsubmit =async (e) => {
        e.preventDefault();
        const {category1 : {value}} = e.target;
        const {data} = await axios.post('/category1/create', {name : value})
        if (data.state === 200) {
            location.reload();
        }
    }
</script>
</html>