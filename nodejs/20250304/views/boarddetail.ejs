


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    상세 페이지
    <form action="/delete?index=<%= index %>" method="post" >

        <%= board.title %>
        <%= board.content %>
        <img src="<%= board.imgPath %>" alt="">
        <button onclick="Ondelete('<%= board.imgPath %>')">삭제</button>
        <button type="button" onclick="onclickUpdate('<%= index %>')">수정</button>
    </form>

</body>
<script>
    const onclickUpdate = (i) => {
       
        location.href = `update?index=${i}`
    }
    const Ondelete = (i) => {
        location.href = `update?index=${i}`
    }
</script>
</html>